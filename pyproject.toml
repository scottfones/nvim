[project]
name = "nvim-python-provider"
version = "0.1.0"
description = "Python provider environment for Neovim (pynvim)."
requires-python = ">=3.14"
dependencies = ["pynvim"]

[tool.uv]
package = false
